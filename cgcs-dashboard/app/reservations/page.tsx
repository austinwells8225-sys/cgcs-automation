export const dynamic = "force-dynamic";

export default function ReservationsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold text-cgcs-ink">Reservations</h1>
      <p className="mt-2 text-sm text-cgcs-mute">
        Reservations list view — coming next. The data is ready (status, lead, deadlines, category).
        Will render a filterable table with quick category re-tagging.
      </p>
    </div>
  );
}
